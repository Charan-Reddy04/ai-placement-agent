/**
 * Safe setup alias. Use `npm run seed:demo` for a ready-to-review job.
 * This command intentionally does not wipe MongoDB. Real student/user data
 * is the source of truth for the placement platform.
 */
import "./seed-demo.js";
